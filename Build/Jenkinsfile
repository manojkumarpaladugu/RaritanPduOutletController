pipeline {
    agent {
        dockerfile {
            dir         'Build'
            filename    'Dockerfile'
        } // Use Dockerfile as the agent for the pipeline
    }
    stages {
        stage('Build') {
            steps {
                sh './UbuntuBuild.sh'
            }
        }
    }
}